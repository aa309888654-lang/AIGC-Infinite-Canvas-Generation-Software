import React from 'react';

const FullPageTechBackground: React.FC = () => {
  return <div style={{ position: 'fixed', top: 0, left: 0, width: '100%', height: '100vh', zIndex: -1, background: '#000' }} />;
};

export default FullPageTechBackground;
