/**
 * PaymentSettings — 支付设置（存根）
 * 登录与会员系统已移除。
 */
import React from 'react';

const PaymentSettings: React.FC = () => {
  return <div className="p-4 text-gray-500">支付设置功能已移除</div>;
};

export default PaymentSettings;