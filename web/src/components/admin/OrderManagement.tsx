/**
 * OrderManagement — 订单管理（存根）
 * 登录与会员系统已移除。
 */
import React from 'react';

const OrderManagement: React.FC = () => {
  return <div className="p-4 text-gray-500">订单管理功能已移除</div>;
};

export default OrderManagement;