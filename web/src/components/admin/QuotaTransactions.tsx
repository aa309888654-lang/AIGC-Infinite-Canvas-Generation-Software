/**
 * QuotaTransactions — 配额交易记录（存根）
 * 登录与会员系统已移除。
 */
import React from 'react';

const QuotaTransactions: React.FC = () => {
  return <div className="p-4 text-gray-500">配额交易功能已移除</div>;
};

export default QuotaTransactions;