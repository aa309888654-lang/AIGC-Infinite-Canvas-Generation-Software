/**
 * pricing-rules — 定价规则（存根）
 *
 * 登录与会员系统已移除，此文件保留空实现以维持编译兼容。
 */

/** 计算视频预期积分 — 返回 0 */
export function calculateVideoExpectedPoints(_modelId?: any, _resolution?: any, _duration?: any, _membershipLevel?: any): number {
  return 0;
}

/** 计算视频每秒积分 — 返回 0 */
export function calculateVideoPointsPerSecond(_modelId?: string, _resolution?: string, _options?: any): number {
  return 0;
}