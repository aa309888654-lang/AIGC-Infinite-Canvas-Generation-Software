/**
 * video-pricing-table — 视频定价表（存根）
 *
 * 登录与会员系统已移除，此文件保留空实现以维持编译兼容。
 */

/** 获取配置的视频费率 — 返回默认值 0 */
export function getConfiguredVideoRate(_modelId?: string, _resolution?: string, _options?: any): number {
  return 0;
}